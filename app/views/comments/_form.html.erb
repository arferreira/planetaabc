<%= form_for(@comment,:html => {:id =>"commentform"}) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h2>Deixar um comentario</h2>
  <p>
    <%= f.label :user_name %><span class="color2">*</span>
    <%= f.text_field :user_name %>
  </p>
  <p>
    <%= f.label :email %><span class="color2">*</span>
    <%= f.text_field :email %>
  </p>
  <p>
    <%= f.label :site %><span class="color2">*</span>
    <%= f.text_field :site %>
  </p>
  <p>
    <%= f.text_area :body %>
  </p>
  <p>
     <%= f.submit "Adicionar Comentário", :class => "button" %>
  </p>
  <div class="cl"></div>
   <%= f.hidden_field :post_id %>
<% end %>
